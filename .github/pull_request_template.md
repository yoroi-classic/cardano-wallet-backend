## What this changes

<!-- A short, plain description of what this PR does and why. -->

## Testing

<!-- How this was verified. Every feature needs happy, unhappy, and regression coverage. -->

- [ ] Unit tests cover the happy path
- [ ] Unit tests cover the unhappy paths (errors, timeouts, malformed input)
- [ ] Regression assertions guard the response/mapping shape
- [ ] `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm run build`, and `npm run test:coverage` pass locally

## Versioning

- [ ] `package.json` version bumped (semver) and `CHANGELOG.md` updated

## Related issues

<!-- Reference the issues this PR addresses. Use `Closes #N` when it fully resolves an
issue, otherwise `Part of #N` and leave a progress comment on the issue. -->

## Notes for the reviewer

<!-- Anything worth calling out: tradeoffs, follow-ups, things intentionally left out. -->
